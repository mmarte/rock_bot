# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Reel output files — download from GitHub Actions artifacts instead
reels/

# Logs (local only)
cron.log

# NOTE: posts.json and log.json ARE committed to the repo
# The GitHub Actions bot updates them automatically via git push
